server:
  port: 8082
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka
      register-with-eureka: true
      fetch-registry: true
spring:
  application:
    name: spring-eureka-user